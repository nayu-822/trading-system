# AGENTS.md

## 目的
本ファイルは、本リポジトリにおけるコード変更時の統一ルールを定義する。
すべてのコード生成・修正は本ルールに従うこと。

---

## 1. 基本方針

- 変更は最小差分で行う（不要なリファクタ禁止）
- 既存設計・責務分離を壊さない
- 不明点は推測で補完せず、前提として明示する
- 説明は日本語で行う
- 既存コードのスタイル・命名規則に従う

---

## 2. アーキテクチャ遵守（重要）

以下の分離を厳守すること：

- 売買ロジック（trading-engine）
- 通知処理（notification-service）
- GUI（gui-tool）

### 禁止事項
- 売買ロジックから直接Discord送信を行うこと
- GUIから売買ロジックを直接操作すること
- モジュール間の密結合

---

## 3. 実装ルール

### 3.1 共通

- 新規コードには型注釈を付与する
- 例外は握りつぶさない（ログ出力必須）
- ハードコード禁止（設定ファイルへ）
- 関数は単一責務にする
- 副作用を最小限にする

---

### 3.2 ログ

- 取引ログとシステムログは分離する
- ログは必ず記録する（失敗時も含む）
- printは禁止（loggerを使用）

---

### 3.3 非同期処理

- 通知処理は必ず非同期で実行する
- 売買処理は通知処理に依存してはいけない

---

### 3.4 設定管理

- モード（paper / live）は設定ファイルで切替
- 環境依存値は設定ファイルまたは環境変数に分離

---

## 4. ドメイン固有ルール（自動売買）

### 4.1 売買処理

- 売買ロジックと発注処理を分離する
- ペーパートレードと実売買は完全に切替可能にする
- 誤発注を防ぐための安全チェックを必ず入れる

---

### 4.2 戦略

- トレンド戦略とレンジ戦略は完全に独立
- 同一ポジションを複数戦略で共有しない

---

### 4.3 データ

- マーケットデータ取得とロジックを分離する
- 計算ロジックは再利用可能にする

---

### 4.4 バックテスト

- 本番コードとバックテストコードを混在させない
- 同一ロジックを使い回せる設計にする

---

### 4.5 パラメータ最適化

- 本番売買ループとは完全に分離
- オフライン処理として実装する

---

## 5. スケジューリング

- 市場時間制御は共通モジュールで管理する
- cron依存の処理をロジックに埋め込まない

---

## 6. 出力フォーマット（必須）

コード変更時は必ず以下の形式で説明すること：

1. 変更内容
2. 変更理由
3. 影響範囲
4. テスト観点
5. 未確認事項

---

## 7. テスト

- 変更箇所にはテストを追加または更新する
- 既存テストを壊さない
- テスト不能な場合は理由を明示する

---

## 8. 禁止事項

- 無関係なファイルの変更
- 大規模な構造変更（指示がない限り）
- ロジックの仕様変更（明示的な要求なし）
- パフォーマンス悪化を伴う変更

---

## 9. レビュー

コードレビューは `code_review.md` に従うこと。


## 10. ドキュメント更新ルール

- 環境構築、運用手順、実行方法に変更がある場合は、関連する docs を必ず更新する
- 再現手順が必要な作業では、docs/setup.md または関連ドキュメントを同時に更新する
- ドキュメントは初見の開発者が再現できる粒度で記載する


## 参照ドキュメント

詳細ルールは以下を参照すること：

- docs/coding_rules.md
- docs/architecture.md