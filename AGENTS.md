# AGENTS.md

## 目的
本ファイルは、本リポジトリにおけるコード生成・修正時の行動ルールを定義する。
設計や仕様の詳細は以下を参照すること。

---

## 1. 参照ドキュメント

実装判断は以下を優先順位順に参照すること。

1. docs/system_specification.md
2. docs/design_specification.md
3. docs/coding_rules.md
4. AGENTS.md

コードレビュー時は code_review.md を参照すること。

---

## 2. 基本ルール

- 変更は最小差分で行う
- 不要なリファクタリングは禁止
- 不明点は推測で補完せず「前提」または「未確認事項」として明示する
- 説明・コメント・レビューは日本語で行う
- 既存コードの命名規則・スタイルに従う
- 実装前に責務の所属（どの層・どのプロセスか）を必ず判断する
- 仕様変更が必要な場合はコードを変更せず先に指摘する
- 一時的な回避コードではなく、恒久的な設計に沿った実装を行う

---

## 3. 実装時の遵守事項

- 実装の詳細ルールは docs/coding_rules.md に従う
- 設計仕様書の責務分離を崩さない
- 設計と異なる実装が必要な場合は変更前に指摘する
- 変更は要求範囲に限定する

---

## 4. 変更時の必須出力

コード変更時は必ず以下を出力すること。

1. 変更内容
2. 変更理由
3. 影響範囲
4. テスト観点
5. 未確認事項
6. コミットメッセージ

### コミットメッセージルール

- 先頭は `英単語1つ + ":" + 半角スペース`
- 変更種別が分かる prefix を付ける

例:
- add: 注文状態モデルを追加
- update: 設定読込処理を改善
- fix: 部分約定時の状態更新不整合を修正
- refactor: ログ出力処理を整理
- test: ロット管理の単体テストを追加
- docs: 設定ファイル説明を更新

---

## 5. ドキュメント更新

- 実装変更が仕様に影響する場合は system/design の更新を検討する
- 実装ルールに影響する場合は coding_rules.md の更新を検討する
- レビュー基準に影響する場合は code_review.md の更新を検討する

---

## 6. 禁止事項

- 無関係なファイルの変更
- 明示指示のない大規模変更
- ロジックの仕様変更
- サイレントエラー
- 設計責務を跨ぐ実装
- ハードコードによる暫定対応

---

## 7. Python 実行ルール

- Python コード、テスト、スクリプト、フォーマッタ、リンタは必ず Pipenv 仮想環境経由で実行する
- python / pytest / pip を直接実行せず、原則として python -m pipenv ... を使用する
- pipenv が PATH にない環境を想定し、pipenv ... より python -m pipenv ... を優先する
- 原則として以下の形式を使用する
  - python -m pipenv run python <script>
  - python -m pipenv run pytest
  - python -m pipenv run black .
  - python -m pipenv run isort .
  - python -m pipenv install <package>
  - python -m pipenv install --dev <package>
- 仮想環境の参照先は .venv に定義された相対パスを使用する
